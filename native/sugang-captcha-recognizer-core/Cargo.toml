[package]
name = "sugang-captcha-recognizer-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "symbols"
