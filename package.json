{
  "name": "sugang-client",
  "version": "0.1.0",
  "private": true,
  "description": "Stateful Sugang course client with automatic CAPTCHA entry",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "sugang-enter": "src/enter.js"
  },
  "scripts": {
    "build:native": "node scripts/build-native.js",
    "benchmark:native": "node scripts/benchmark-recognizer.js",
    "decode": "node src/decode.js",
    "evaluate": "node src/evaluate.js",
    "prepare": "npm run build:native",
    "preenter": "npm run build:native",
    "enter": "node src/enter.js",
    "test": "npm run build:native && cargo test --manifest-path native/sugang-captcha-recognizer-core/Cargo.toml && node --test"
  }
}
